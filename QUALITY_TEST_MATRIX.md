# OptiLLM Quality Test Matrix (v0)

Use this matrix to evaluate OptiLLM routing quality with the **current** handles.

## Active handles
- _None currently registered._

## Suggested prompts
- “Design a Python service that syncs MLX registry updates to LiteLLM, with a safe rollback plan. Outline modules and interfaces.”
- “Summarize the key tradeoffs between router-only and MOA-heavy OptiLLM selectors for local stacks.”
- “Draft a migration plan for moving two MLX models into default boot ensemble without breaking LiteLLM aliases.”
